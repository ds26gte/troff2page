# last modified 2020-11-07

DEST=../lua/troff2page.lua

rm -f $DEST

for f in *.lua; do
  cat $f | grep -v '^-- last modified' >> $DEST
  echo >> $DEST
done

DEBUG=false

echo "Single_pass_p = $DEBUG" >> $DEST

echo "troff2page(...)" >> $DEST

chmod +x $DEST

